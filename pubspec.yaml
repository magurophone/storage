name: storage_monitor_new
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0" # Dart SDKのバージョン範囲を明確に指定

dependencies:
  flutter:
    sdk: flutter

  # UI関連
  cupertino_icons: ^1.0.6
  dropdown_button2: ^2.3.9

  # ストレージ・デバイス情報
  path_provider: ^2.1.5
  device_info_plus: ^11.3.3
  storage_space: ^1.1.0    # ディスク容量取得用プラグインを追加

  # 権限管理
  permission_handler: ^11.1.0

  # HTTP通信
  http: ^1.1.3

  # ローカルストレージ
  shared_preferences: ^2.2.0

  # バックグラウンド処理
  flutter_foreground_task: ^8.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0  # 推奨される最新のLintルールセット

flutter:
  uses-material-design: true